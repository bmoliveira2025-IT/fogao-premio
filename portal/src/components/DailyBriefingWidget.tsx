"use client";

// This widget is now effectively deprecated as the trigger 
// has been moved to BrandingHeader.tsx.
// Keeping the file to avoid breaking imports in page.tsx, 
// but it renders nothing.

export default function DailyBriefingWidget() {
    return null;
}
